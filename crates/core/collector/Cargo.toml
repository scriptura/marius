
[package]
name = "marius-collector"
description = "Lock-free bit-vector collector for the Marius reactive engine. Pure Core, no async runtime."
readme = "README.md"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
authors.workspace = true
keywords = ["ecs", "lock-free", "bit-vector", "marius"]
categories = ["concurrency", "data-structures"]

# Aucune dépendance externe.
# Tokio → marius-render (Dispatcher).
# SQLx  → marius-projection (trait Projection).
[dependencies]
