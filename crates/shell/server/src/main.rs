// marius-server — point d'entrée du processus.
// Shell std : initialise le pool SQLx, les Collectors, les Dispatchers,
// le listener LISTEN/NOTIFY, et le serveur Axum.

#[tokio::main]
async fn main() {
    // Stub — sera rempli quand les crates Core et Shell seront opérationnels.
    println!("Marius server starting...");
}
